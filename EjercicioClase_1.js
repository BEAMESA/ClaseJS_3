'use strict';

// Función lenght -> Calcula la longitud de una cadena de texto

const saludo = 'Hola'
const numeroletras = saludo.length
console.log (numeroletras)

// Función concat(): concatena varias cadenas de texto

const mensaje = saludo.concat(' ,buenos dias');
console.log(mensaje)

// Función toUpperCase(); transforma los caracteres en mayusculas

const saludo2 = saludo.toUpperCase('que tal');
console.log (saludo2)

// Función toLowerCase(): transforma los caracteres en minusculas

const